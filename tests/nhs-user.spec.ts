import { test, expect } from "@playwright/test";
import { headingCheck, loaderRemove } from "../src/Helper/e2e-test-utils";

test("NHS user registration", async ({ page }) => {
  //   await page.goto("http://localhost:3000/Participants/Options");
  //   await page.getByRole("button", { name: "Accept additional cookies" }).click();
  //   await page.getByRole("button", { name: "Hide cookie message" }).click();
  //   await page.getByRole("link", { name: "Continue to NHS login" }).click();
  //   await page.getByLabel("Email address").click();
  //   await page.getByLabel("Email address").click();
  //   await page
  //     .getByLabel("Email address")
  //     .fill("testuserlive@demo.signin.nhs.uk");
  //   await page.getByRole("button", { name: "Continue" }).click();
  //   await page.getByRole("button", { name: "Continue" }).click();
  //   await page.getByLabel("Password", { exact: true }).click();
  //   await page.getByLabel("Password", { exact: true }).fill("Passw0rd$1");
  //   await page.getByRole("button", { name: "Continue" }).click();
  //   await page.getByLabel("Security code", { exact: true }).click();
  //   await page.getByLabel("Security code", { exact: true }).fill("190696");
  //   await page.getByRole("button", { name: "Continue" }).click();
  //   await loaderRemove(page);
  //   await headingCheck(
  //     page,
  //     "Consent to process your data and be contacted by Be Part of Research"
  //   );
  //   await page
  //     .getByLabel(
  //       "I confirm that I have read and understood the Be Part of Research Privacy Policy."
  //     )
  //     .check();
  //   await page
  //     .getByRole("button", { name: "Yes, I consent and wish to register now" })
  //     .click();
});
