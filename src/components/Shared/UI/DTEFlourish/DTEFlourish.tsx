import styled from "styled-components";

const DTEFlourish = styled.div`
  margin: 0 0 35px 0;
  width: 50px;
  border-bottom: ${(props) => `5px solid ${props.theme.NIHR.Orange}`};
`;

export default DTEFlourish;
