import React from "react";

// interface CreateAccountProps {
// //   text?: string;
// //   colour?: string;
// }

// const useStyles = makeStyles(() => ({

// }));

function ParticipantsHome(/* {
   text = "Loading",
   colour = useTheme().palette.nhsnavy.dark,
}: CreateAccountProps */) {
  //   const classes = useStyles();
  return (
    <div>
      <h1>Participants landing page</h1>
    </div>
  );
}

export default ParticipantsHome;
